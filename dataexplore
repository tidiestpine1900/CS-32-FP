def main():
    # Load Spotify songs (.zip file)
    import json

    f = open('sample.json')
    data = json.load(f)

    print("hi")

    print(data.keys())
    # pulls first song of first playlist 
    print(data["playlists"][0]["tracks"][0])

    for i in range(len(data["playlists"])):
        print("\n\nPlaylist #: ", i + 1)
        for j in range(len(data["playlists"][i]["tracks"])):
            print(data["playlists"][i]["tracks"][j]["artist_name"])

    # Display color options and grab user 
    
    # Find a path from start to goal, if one exists
    

    # Print degrees of separation and path for user
    
if __name__ == '__main__':
    main()
